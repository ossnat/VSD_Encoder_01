# Per-pixel mean over frames [35, 46) with per-trial baseline z-score.
#
# Frame indexing is 0-based and half-open throughout:
#   analysis:  [35, 46) → frames 35..45 inclusive
#   baseline:  [5, 26)  → frames 5..25 inclusive (per-pixel mean/std)
# Each pixel is z-scored as (x - mean) / max(std, eps) before averaging.
# Aliases: normalization may also be written as zscore_baseline; none/raw = off.

window_id: win_0035_0046_zscore
start_frame: 35
end_frame: 46
avg_method: mean
normalization: baseline_zscore
baseline_start_frame: 5
baseline_end_frame: 26
baseline_std_eps: 1.0e-8
